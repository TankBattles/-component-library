import React from 'react';
import Layout from '@keith/layout';

export default () => {
  return (
    <div>
      <p>该 Button 都会添加一个 emoji 前缀 😀</p>
      <Layout title="Antd Button"></Layout>
    </div>
  );
};
