import React from 'react';
import Button from '@keith/button';

export default () => {
  return (
    <div>
      <p>该 Button 都会添加一个 emoji 前缀 😀</p>
      <Button title={'Antd Button'}></Button>
    </div>
  );
};
